{
  "name": "pi-jev-memory",
  "version": "0.1.0",
  "description": "pi extension: jev-classified, cache-aware memory routing (context / file / forget)",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "replay": "node --import tsx eval/replay.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "typesafe",
    "jev",
    "memory"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@typesafe-ai/sdk": "^0.6.0",
    "typebox": "^1.3.33"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
