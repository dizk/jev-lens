---
description: Show what jev-lens compressed in recent sessions and how many tokens it kept out of the prompt
---
Call the jev-lens `stats` MCP tool (no arguments) and present its output to the user unchanged, as a code block. If the tool is not available, say that the jev-lens MCP server is not connected and that `/mcp` shows why.
