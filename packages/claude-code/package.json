{
  "name": "claude-jev-lens",
  "version": "0.5.0",
  "private": true,
  "description": "Claude Code plugin: jev picks what the model gets to see of large Read, Bash and Grep results before they are sent; the full output stays recallable through an MCP tool.",
  "author": "Didrik Rognstad",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dizk/jev-lens.git",
    "directory": "packages/claude-code"
  },
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "jev-lens": "^0.5.0"
  }
}
